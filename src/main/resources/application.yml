
spring:
  application:
    name: url-shortener

server:
  port: 8080
  servlet:
    context-path: /

app:
  base-url: http://localhost:8080

logging:
  level:
    root: INFO
    com.example.urlshortener: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
